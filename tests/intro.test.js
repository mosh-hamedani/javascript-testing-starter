import { describe, it, test, expect } from "vitest";

describe("max", () => {
  it("should return the first argument if it is greater.", () => {
    //Arrange
    const a = 2;
    const b = 1;
    //Act
    //Assert
  });
});
